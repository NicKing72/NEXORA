# Cross-application tests

This directory is reserved for future end-to-end and contract tests spanning both applications. Current unit and smoke tests live beside their application, such as `apps/api/tests/test_health.py`.
